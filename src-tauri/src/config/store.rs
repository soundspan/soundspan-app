// Store keys used by soundspan-app via tauri-plugin-store.
//
// Store file: config.json (in OS app data directory)
//
// Keys:
//   - "instance_url"            (String) — user's soundspan instance URL
//   - "wasapi_exclusive_enabled" (bool)  — WASAPI exclusive mode toggle (Windows only)
